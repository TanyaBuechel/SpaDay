@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<div id="clientProfile">
</div>
<div id="servicesMenu">
</div>

<h1>Fancy Spa</h1>

<div id="clientProfile">
    @*Add h3 that says Client Profile 
      and display name from the ViewBag property in a p tag*@
    <h3>Client Profile</h3>
    <p>@ViewBag.name</p>
</div>

<div id="servicesMenu">
    <h5>Facial Treatments</h5>
    @*Pass in skintype variable*@
    <p>Your @ViewBag.skintype skin type is best suited to the follow facial types:</p>
    <ul>
        @*Iteratively add the values in appropriateFacials to an unordered list*@
        @foreach (string facials in ViewBag.appropriateFacials)
        {
            <li>@facials</li>
        }
    </ul>

    <h5>Nail Services</h5>
    @*Add conditional to display different nail service descriptions*@
    @if(ViewBag.manipedi == "manicure")
    {
        <p>Our manicure is a great way to spend 30 minutes of your day! We use shea butter hand cream and the finest gel polish.</p>
    }
    else if(ViewBag.manipedi == "pedicure")
    {
        <p>Relax for 45 minutes in pure luxury! Our massage chairs and experienced nail techs are here to get your feet in shape for sandal season!</p>
    }
</div> 

<div class="nail-colors">
    @foreach(string color in ViewBag.nailColors)
    {
        <div class="color-block" style="background-color:@color"></div>
    }
</div>


